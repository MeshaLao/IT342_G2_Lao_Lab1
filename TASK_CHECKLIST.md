# TASK CHECKLIST

## DONE
- Repository created and cloned
- MySQL database created (Lao_DB)
- Backend project setup 
- User entity created 
- UserRepository created with JPA methods
- DTOs created (RegisterRequest, LoginRequest, AuthResponse)
- JWT TokenProvider implemented
- JwtAuthFilter created for request authentication
- Spring Security configuration completed (SecurityConfig)
- BCrypt password encryption configured
- AuthService implemented (register and login logic)
- User registration API (POST /api/auth/register)
- User login API (POST /api/auth/login)
- Password encryption (BCrypt)
- Protected user profile API (GET /api/user/me)
- MySQL database integration
- Backend tested and running on port 8080
- Frontend project setup (React with proper folder structure)
- AuthContext and useAuth hook created
- Auth service for API calls created
- Reusable UI components created (Button, FormInput, Alert, Logo)
- Web UI pages: Register page
- Web UI pages: Login page
- Web UI pages: Dashboard/Profile page
- Web UI pages: Logout functionality
- Frontend styling completed 
- Frontend connected to backend APIs

## IN-PROGRESS
- Testing registration → login → dashboard → logout flow
- redesigning the UI

## TODO
- README.md with setup instructions
- Mobile app development (Lab 3 - next session)